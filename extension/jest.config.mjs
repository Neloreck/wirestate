// Thin re-export of the dedicated extension Jest config (canonical copy lives in cli/test).
// Its sole purpose is IDE ergonomics: WebStorm auto-detects the nearest `jest.config.*`.
export { default } from "../cli/test/jest.extension.config.mjs";
