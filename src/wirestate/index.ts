/**
 * Compatibility entry for the unscoped `wirestate` package: re-exports `@wirestate/core` and `@wirestate/react`.
 * Use the `wirestate/mobx` and `wirestate/signals` subpaths for the reactivity bindings.
 *
 * @packageDocumentation
 */

export * from "@wirestate/core";
export * from "@wirestate/react";
