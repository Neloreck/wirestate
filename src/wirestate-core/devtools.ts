/**
 * DevTools integration entrypoint for Wirestate: the {@link DevToolsPlugin} an application registers
 * to expose its container tree to an inspector backend (a Chrome extension or a standalone dev panel).
 *
 * @packageDocumentation
 */

export { DevToolsPlugin, type DevToolsPluginOptions } from "./plugin/devtools/devtools-plugin";
